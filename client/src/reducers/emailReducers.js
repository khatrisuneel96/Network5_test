let emails = (emails = [], action) => {
    switch (action.type) {                 //shows what to do in each function case     
        default:
            return emails
    }
}
export default emails