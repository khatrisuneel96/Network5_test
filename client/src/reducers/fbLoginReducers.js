let Fblogin = (Fblogin = [], action) => {
    switch (action.type) {                 //shows what to do in each function case     
        default:
            return Fblogin
    }
}
export default Fblogin
