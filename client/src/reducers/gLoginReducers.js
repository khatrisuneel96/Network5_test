let Glogin = (Glogin = [], action) => {
    switch (action.type) {                 //shows what to do in each function case     
        default:
            return Glogin
    }
}
export default Glogin